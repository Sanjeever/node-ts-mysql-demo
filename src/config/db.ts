import { ConnectionConfig } from "mysql";

const MYSQL_CONFIG: ConnectionConfig = {
  host: "localhost",
  user: "root",
  password: "",
  port: 3306,
  database: "mysql",
};

export { MYSQL_CONFIG };
